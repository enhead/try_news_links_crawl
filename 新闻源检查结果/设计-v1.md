





## 设计-v1

> 初版设计
>
> 将上面做一次阶段性的总结设计
>
> 这东西就是很难，而且也别想着一次性做好，目前看这些千奇百怪的样本不太可能，所以估计只能尽量去善用类了
>
> 然后这些流程看看要不要涉及成可有可无的
>
> 哎，不行还要学，还是太菜了，这种架构的设计感觉还是不行





> 不行了，有点乱，再明确下需求：
>
> 这里会适当的忽略掉一些特殊情况，不然样本太大，很难一次性直接覆盖
>
> - 目前主要获取的是新闻链接，即可
>
> - 这里主要针对的是能够使用HTTP请求的，那些可能要用到自动化工具的，这里可能会选择适当忽略，后续再看能否统一，我感觉最好不要
>
> - 由于是爬虫，同一个新闻源不追求高并发，不同新闻源倒是可以并发
>
> - 目前设计主要针对是增量，全量估计需要使用另外的策略
>
> - ==**目前主要针对 类别->页，不然其他场景过于复杂了，根本覆盖不过**==
>
>   > 这个问题真的太过复杂了，有非常多的特殊情况





> 初步构思：
>
> - 我现在是赶毕设，最多只能把整个流程串起来写出来就不错了，但是这个功能其实**非常看代码质量**
>   - 目前我先串一下，由于时间问题，我不一定会全部实现出来
> - 希望最后交付的最好是一个个HTTP请求，一开始不是没有关系，这样比较好解耦分层，后续设计高并发也容易点：这个后续看看
> - **先不用配置文件**了，以目前来看，无论怎么设计感觉都会有点缺陷，这里干脆面向子类，不过还是不要忘记，看看能不能尝试下
> - 尝试下能不能用多路归并的思想，当前能够看出下一个：也不用硬尝试，先实现出基本功能



> 都是参考依然没有想好
>
> 目前主要参考的是v2.0最后的哪个方案，但是还是差很多









新闻源类：目前设想的职责是只需要提供后续执行必要的参数，不参与具体的执行

> 感觉可以先搞一个基类
>
> 这里目前先不配置，后续

- 生成需要的task任务：

  - 涉及到短路逻辑：大方向是按分类先分，分完后会按页码一页页来

    > 有点像循环的++，最外层先++不断往内
    >
    > 在想这个操作是否要放在这里，要好好设计，不然后续设计并发不好弄
    >
    > 还在想要不要弄成多路归并的形式，每个类就是对应的一层，但是这样并发似乎不是很好弄
    >
    > 最后的根节点就是一个HTTP请求
    >
    > 这个感觉很有可能需要一个树的结构

    > > task任务节点：当前层的任务，主要是他可能会两三层，主要为两层`(类别，页码)`
    > >
    > > 后续重试啊，断点什么的也是存这个的坐标信息
    > >
    > > 这个是一个树结构：`(类别，页码)`
    > >
    > > - 树节点：一个个的分类
    > >   - 树节点：一页页
    > >     - 叶子：最终的HTTP请求
    > >
    > > 记录断点是可以是：`(类别数组下标，页码下标)`
    > >
    > > 感觉可以坐标化，抽象成维度，这样好扩展很多
    >
    > 这里可能会出现一种情况，类别都遍历，但是想页码这种连续出现的相同太多，直接停止

  - 涉及到生成HTTP请求

- 提取新闻链接对应的定位：

  - 格式：主要HTML，可能其他格式，例如json这种
  - 这个主要是后续请求完后，内容的提取，变更检查也需要用到

- 防重的KEY：默认字符串，目前默认是URL，目前这个就够了

  > 数据库感觉可以增加一个这个字段





编排器：相当于服务层，串起整套流程

> 后续高并发的优化，对这里下手就行
>
> 目前还是基础流程，大概就是这样
>
> 这里具体层的设计还是没有想好

1. 遍历所有新闻源

   1. 新闻源变更检查：就是看那些定位能不能对上

   2. 新闻源生成一个个的task任务，这个task任务的根节点就是HTTP请求：

      > 这里后续在想并发，如果想要并发，估计需要多设计一些东西了

      1. 请求器
      2. 解析出链接
      3. 批量判断存在性
      4. 短路判断：是否提前结束，主要针对的是当前task
      5. 存储（不一定放在这里，后续可能会进行批量操作）
      6. 更新断点（不一定放在这里）





> 设计细节：
>
> > 具体搜索下之前的
>
> - 步骤是否可插拔：很容易过度设计
> - 多路短路
>   - 页面级短路
>   - 连续存在阈值
> - 存在性方案：当前新闻链接是否已经存在，这里大部分是否按url感觉就够了
> - 断点重爬：感觉这个挺难实现的
> - 请求层的封装
>   - 发出HTTP请求
>   - 重试、限流、错误处理
> - 并发策略





---

> 哎，真难呀
>
> 这里再次迭代，上面有太多会卡住的点了
>
> 我就不要妄想能够一下子覆盖所有的情况，感觉可能性不大呀
>
> 目前感觉就是很多情况根本没法简单覆盖，可能就是为了那个百分之一的特殊情况，但是需要付出特别多的代价
>
> 目前为了后续的拓展性，最好还是面向接口开发，如果当前方法不行，就让agent自己实现
>
> 我现在这里感觉还不够好，估计还要多做几层抽象，目前做出的抽象肯定是非常非常面向抽象的，现在就面向具体非常难搞，可以说根本搞不了。怎么感觉抽象也好难
>
> 
>
> 目前这个主要针对能够HTTP请求的情况，这里不用浏览器自动化工具
>
> 不要想太多的特殊情况了，太庞大了
>
>
> 好复杂，有点想上DDD了





---





新闻源基类：用于提供出新闻源运行过程中的必要参数

- pass，大部分还没想好
- 构造当前HTTP的请求
- 检查是否变更
- 获取链接



编排器基类：

- 遍历对应 新闻源子类 的方法，去获取对应的新闻链接，不同的 新闻源基类 可能有不同的方法
- 遍历多个新闻源子类的方法





默认的新闻源基类：主要应对的情况就是`(类别，页码)`的情况：

- 属性：
  - 新闻链接元素对应的详细位置
- 方法：
  - 检查变更(响应)：用元素位置
  - 获取链接(响应)：用元素位置



> 坐标化感觉是后续拓展最方便，但是好难呀

> 大场景：新闻链接提取
>
> 此处我想抽象成任务树
>
> 再次梳理，这个任务节点最终想要的结果是HTTP请求，至于返回的参数、响应或者相应解析后的新闻链接等等目前待定，看看似乎哪种
>
> 这个任务节点按层的划分的，比如我们可能第一层是分类、第二层是页码，直到叶子最终会汇总到我所有需要的HTTP请求参数
>
> 这里复杂的点在于比如我处在当前分类，会根据下面的页码层的返回节点进行截断，不在往下，例如页码(1,2,3)中的数据在数据库中都存在这时候当前分类就在继续，直接下个分类，这里我该如何设计
>
> 后续我可能会同层的采用并发
>
>
> 原本的好复杂，有点想直接让任务节点输出我想要获取的数据了

任务结点基类：

> 树+坐标
>
> 展开可以当成一个子树
>
> 最终获取HTTP请求的参数是在叶子
>
> 看看能否抽象层的概念

- 属性：
  - 节点状态
  - 当前结果
- 方法：
  - 执行当前子树（其实就是遍历当前子树的所有节点，直到叶子），会返回叶子的情况，后续做断连处理
    - 封装下结果
    - ？待定：参数，可以用字典上下文，分装一下必要数据，后续好拓展，就是不太好维护
  - 根据属性判断当前节点是否停止，返回





叶子节点（任务结点基类）：这里就会跟HTTP请求打交道了

- 感觉这里可以去请求，也可以只是返回请求需要的数据，目前偏向返回需要的数据，不然职责太重了



---



> 这里我可能迫不得已使用一些字典作为参数，后续看看如何优化



> 上面的有点难受，细节太多了，目前打算从顶至底，这样复杂度比较低，而且好扩展
>
> 感觉可以先整理好所有需要的方法，再做归纳整理
>
> 最好先想方法，后面再去想属性，会比较好点
>
>
> 屏蔽底下的越多细节越好



先想好基类吧：



总目标：

获取新闻源的新闻链接服务接口：

- 增量获取当前新闻源新闻链接，这个返回就需要跟数据库产生关联了
  - 这里再想要不要直接插入数据库中，还是只是获取到数据，后续再插入









> 下面两个感觉可以放在一起
>
> 感觉这个可以放在配置类中
>
> 
>
> 这两个方法是后面的基石，后面基本所有的东西都要围绕这个方式
>
> 我需要想想这个如何泛化为整个新闻源的

- 发送单个新闻源的请求：
  - 返回的是响应还是请求需要的参数待定，目前偏向响应，这样能够统一
- 解析获取的响应：
  - 检查变更也可以放在这里
  - 解析出新闻链接



- 数据库的存在性检查



现在到实现细节了，上面都是一些顶层设计：



> 感觉函数化更好，而不是成为一种参数
>
> 你页码这种无限增加的，丢给上面的一层做短路判断 也可能是类别这种跟类别映射的，是一种数组的映射关系 还可能需要根据上一层传递的值，去找当前层的值，是一种字典的关系
>
> 适当做取舍，先大体实现
>
> 这里看着最好还是需要有状态



树节点 | 层 | 坐标其中一个维度：**目前没有想好：这个究竟要作为何种概念；以及这个要作为总目标的实现，还是作为参数的生成器**

> 这个需要依赖于上面的配置类
>
> 但是里面的步骤需要去请求，解析数据，然后去检查数据库中的这些数据是否存在或者存在的数目，去做短路，当前节点是否还要继续
>
> 这个逻辑很复杂
>
> 下面这些还只是草稿
>
> 后续这里同层的可能会升级成并发，现在暂时不用管
>
> 这个类需要依赖上面同样的类，参数是一层层传递的，不能跳，固定就是从最上面那层开始的
> 这个参数的如何传递，感觉这个有点难度呀

- 职责：
  - 短路逻辑判断
  - 叶子节点：不一定是叶子节点本身执行，没有想好，但是这个就是需要去调用【单个新闻源的请求】，【解析响应】，【可能是批量判断存在性】（具体逻辑在节点中实现），每个节点会返回一个结果类（涵盖最终需要的新闻链接，以及短路判断需要参数等等）
  - 存储：不确定要不要放在这里、
  - 更新断点：不确定是否要放在这里

（不完整，还在想）

- 属性：
  - 参数传递的dict，供给单个请求时使用
  - 需要拥有新闻源的配置
  - 需要去请求
  - 需要用到数据库的存在性检查
  - 感觉这里可能放下一个【树节点 | 层 | 坐标其中一个维度】
- 方法：
  - 主方法：执行获取当前节点下面所有的新闻链接，此处会记录所有节点的返回结果，这里结果感觉又设计一个数据结构，后面分析可能还需要用到
    - 子方法：当前节点是否需要剪枝判断，根据下面的子节点返回结果慢慢判断



> 这里可以把枚举层、依赖层、顺序层的代码先抽象出来





### 整理总结：



> 自己+AI迭代了快十几轮
>
> 跟上面还是有差异，但是我感觉这个版本还不错，可以采纳
>
> 这里感觉挺牛逼的，让她自己在层内自己内循环，自己做短路，我完全没有想到



> 目前这个设计，差断点重试这个功能，其他基本解决
>
> 这个是for循环，意外的很好处理
>
>
> 这里迫不得已可能会使用一些字典参数，但是我已经很满意了，没有办法，这个东西就是本身就是会一直变化的东西



> > 让claude按我的思路改了很多版
>
> 总目标：
>
> 获取新闻源的新闻链接服务接口：
>
> - 增量获取当前新闻源新闻链接，这个返回就需要跟数据库产生关联了
>   - 这里再想要不要直接插入数据库中，还是只是获取到数据，后续再插入
>
> # 新闻链接爬虫系统 — 完整设计汇总 V2
>
> ------
>
> ## 整体架构
>
> ```
> NewsLinkService
> └── CrawlOrchestrator              # 多源并发调度
>     └── SourceCrawler              # 单源入口，很薄
>         └── Layer链（自驱动）
>             ├── EnumerableLayer
>             ├── DependentLayer
>             └── SequentialLayer
>                 └── CrawlNode      # 单次请求执行单元
>                     ├── HttpAdapter
>                     ├── SourceConfig
>                     └── DbChecker
> ```
>
> ------
>
> ## 配置层
>
> ### SourceConfig
>
> > 算是一个新闻配置基类
> >
> > 这个我是打算专门用于HTTP请求的
>
> 描述一个新闻源"如何请求、如何解析"，纯数据，不含执行逻辑。
>
> ```
> SourceConfig
> ├── 属性
> │   ├── source_id
> │   ├── root_layer: Layer           # Layer 链的入口
> │   ├── request_config              # headers / method / timeout 等
> │   └── parse_config                # CSS选择器 / JSONPath / 正则
> │
> └── 方法
>     ├── build_request(params: dict) → RequestParams
>     │   # 将积累的遍历参数填入 URL 模板，生成完整请求参数
>     │
>     └── parse_response(response) → ParseResult
>         # 从响应中提取新闻链接
>         # ParseResult = { links: List, is_empty: bool }
> ```
>
> ------
>
> ## Layer 层
>
> > 回去执行方法
>
> ### 抽象基类
>
> ```
> Layer（抽象基类）
> ├── 属性
> │   ├── next_layer: Layer | None    # 持有下一层引用
> │   └── key: str                    # 当前层参数名，如 "category" / "page"
> │
> └── 方法
>     ├── execute(accumulated_params: dict) → LayerResult
>     │   # 主方法：获取当前层及以下所有新闻链接
>     │   # 负责遍历本层的值，逐一调用 next_layer.execute()
>     │   # 汇总所有子结果，返回 LayerResult
>     │
>     └── should_prune(results_so_far: List[NodeResult]) → bool
>         # 子方法：根据已积累的子结果判断是否需要短路
>         # 各子类按需覆写
> ```
>
> ------
>
> ### EnumerableLayer（枚举层）
>
> ```
> EnumerableLayer
> ├── 属性
> │   └── values: List                # 固定值列表，如 ["tech", "finance"]
> │
> └── execute 行为
>     # 遍历 values，逐一调用 next_layer.execute(params + value)
>     # 有限集合，自然结束，通常不需要 should_prune
> ```
>
> ------
>
> ### DependentLayer（依赖层）
>
> ```
> DependentLayer
> ├── 属性
> │   ├── mapping: Dict               # 父层值 → 当前层值列表
> │   └── parent_key: str             # 从 accumulated_params 中取哪个 key
> │
> └── execute 行为
>     # 从 accumulated_params 取出父层值
>     # 通过 mapping 查出当前层的值列表
>     # 逐一调用 next_layer.execute(params + value)
>     # 有限集合，自然结束，通常不需要 should_prune
> ```
>
> ------
>
> ### SequentialLayer（顺序层）
>
> ```
> SequentialLayer
> ├── 属性
> │   ├── start: int
> │   └── step: int
> │
> └── execute 行为
>     # 从 start 开始递增
>     # 每次创建 CrawlNode(accumulated_params + {page: current}) 并执行
>     # 每执行完一页调用 should_prune(results_so_far)
>     # 触发剪枝则停止，否则 current += step 继续
> 
>     should_prune 判断依据（可配置策略）：
>     # - 当前页为空页 → True
>     # - 当前页链接全部已存在于 DB → True
>     # - 存在比例超过阈值 → True
> 
> 注意：SequentialLayer 是叶子层，它直接持有 CrawlNode
> ```
>
> ------
>
> ## 执行层
>
> ### CrawlNode（单次请求执行单元）
>
> > 叶子执行的是这里哦，记住啦
>
> ```
> CrawlNode
> ├── 属性
> │   ├── params: dict                # 积累的完整遍历参数
> │   ├── source_config
> │   ├── http_adapter
> │   └── db_checker
> │
> └── 方法
>     └── execute() → NodeResult
>         # 1. source_config.build_request(params)
>         # 2. http_adapter.send(request_params)
>         # 3. source_config.parse_response(response)
>         # 4. db_checker.check_batch(links)
>         # 5. 组装并返回 NodeResult
> ```
>
> ------
>
> ### HttpAdapter
>
> ```
> HttpAdapter
> └── send(request_params) → Response
>     # 唯一真正发出 HTTP 请求的地方
>     # 处理重试、超时、异常封装
> ```
>
> ------
>
> ### DbChecker
>
> ```
> DbChecker
> └── check_batch(links: List) → CheckResult
>     # 批量查询哪些链接已存在
>     # CheckResult = { existing: Set, missing: List, exist_ratio: float }
> ```
>
> ------
>
> ## 数据结构
>
> ```
> NodeResult（叶子层产出）
> ├── links_found                     # 本页解析出的全部链接
> ├── links_new                       # DB 中不存在的新链接
> ├── is_empty                        # 是否空页
> └── exist_ratio                     # 已存在比例，供 should_prune 使用
> 
> LayerResult（每层 execute 的返回值）
> ├── node_results: List[NodeResult]  # 当前层及以下所有节点结果
> ├── new_links: List[Link]           # 汇总后的新链接（已去重）
> └── stats                           # 请求数、命中率等，供上层分析
> ```
>
> ------
>
> ## 调度层
>
> ### SourceCrawler（单源入口，很薄）
>
> ```
> SourceCrawler
> └── crawl() → CrawlResult
>     # 调用 source_config.root_layer.execute({})
>     # Layer 链内部自驱动完成遍历、短路、结果收集
>     # 拿到顶层 LayerResult 后封装成 CrawlResult 返回
> 
> CrawlResult
> ├── source_id
> ├── new_links
> └── stats
> ```
>
> ------
>
> ### CrawlOrchestrator（多源调度）
>
> ```
> CrawlOrchestrator
> └── run(source_configs: List) → List[CrawlResult]
>     # 每个源创建独立 SourceCrawler
>     # 不同源之间并发执行
>     # 同一源内部 Layer 链保持串行
> ```
>
> ------
>
> ### NewsLinkService（对外入口）
>
> ```
> NewsLinkService
> ├── fetch_new_links(source_ids) → List[Link]
> │   # 只获取，不写库
> │
> └── fetch_and_save(source_ids) → SaveResult
>     # 获取后写库（可选封装）
> ```
>
> ------
>
> ## 示例：某新闻源配置
>
> **场景**：一级类别 `[tech, finance]`，二级类别依赖一级，每个二级类别下翻页。
>
> ```
> SourceConfig(
>   source_id = "example_news",
> 
>   root_layer = EnumerableLayer(
>     key    = "cat1",
>     values = ["tech", "finance"],
>     next_layer = DependentLayer(
>       key        = "cat2",
>       parent_key = "cat1",
>       mapping    = {
>         "tech":    ["ai", "mobile"],
>         "finance": ["stock", "crypto"]
>       },
>       next_layer = SequentialLayer(
>         key   = "page",
>         start = 1,
>         step  = 1
>       )
>     )
>   ),
> 
>   request_config = { url: "https://example.com/{cat1}/{cat2}?page={page}", ... },
>   parse_config   = { type: "css", selector: "a.news-title", attr: "href" }
> )
> ```
>
> **执行链路**：
>
> ```
> EnumerableLayer.execute({})
> └── "tech" → DependentLayer.execute({cat1: "tech"})
>     └── "ai" → SequentialLayer.execute({cat1:"tech", cat2:"ai"})
>         ├── page=1 → CrawlNode.execute() → NodeResult
>         ├── should_prune? 否 → page=2
>         ├── page=2 → CrawlNode.execute() → NodeResult
>         └── should_prune? 是 → 返回 LayerResult
>     └── "mobile" → SequentialLayer.execute({cat1:"tech", cat2:"mobile"})
>         └── ...
> └── "finance" → DependentLayer.execute({cat1: "finance"})
>     └── ...
> 
> 每层 execute 完成，LayerResult 向上冒泡汇总
> SourceCrawler 拿到最终结果
> ```
>
> ------
>
> ## 关键设计决策一览
>
> | 问题           | 决策                                              |
> | -------------- | ------------------------------------------------- |
> | Layer 如何驱动 | 自驱动，持有 next_layer 引用，自己调用下一层      |
> | 短路判断归属   | 在 SequentialLayer 内部，每页后调用 should_prune  |
> | traverse()     | 不再需要，逻辑全内聚在 Layer 链中                 |
> | SourceCrawler  | 很薄，只是调用 root_layer.execute({}) 的入口      |
> | 写库时机       | Service 层可选，爬取与持久化解耦                  |
> | 并发粒度       | 源间并发，源内串行（后续同层并发只改对应 Layer）  |
> | 参数传递       | accumulated_params 逐层积累，不跳层               |
> | 配置纯净       | SourceConfig 只含数据描述，Layer 自身承载执行逻辑 |
